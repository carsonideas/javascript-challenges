const pageTitle = document.getElementById ("title");
Console.log (pageTitle);


const pageTitle = document.getElementsByTagName (name);
Console.log (pageTitle);

const pageTitle = document.getElementsByClassNameClassName (name);
Console.log (pageTitle);